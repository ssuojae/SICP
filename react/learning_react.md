## Learning React 


### Chapter 2. Javascripts for React

- Prior to ES2015, the only way to declare a variable was with the `var` keyword.
- A constant is a avariable that cannot be overwritten.
- If a variable is created inside of an `if/else` block, that variable is not scoped to the block.
- With the `let` keyword, we can scope a variable to any code block. Using `let` protects the value of the global variable.
- One thing to be aware if when deciding between a function declaration and a function expression is that function declarations are hoisted and funtion expression are not.
- Since we used the spread operator to copy the array, the `peacks` array is stil

<br/>

### Chapter 3. Functional Programming with Javascript

- However, `Array.push` is not an immutable function. This `addColor` functino changes the original array by adding another field to it. In order to keep the `colors` array immutable, we must use `Array.concat` instead.
- When it's time to remove an item from an array, we should use `Array.filter` over `Array.pop` or `Arrary.splice` because `Array.filter` is immutable.

<br/>

## Chaper 4. How React Works
- 
